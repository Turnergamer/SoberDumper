[package]
name = "rust-dumper"
version = "0.1.0"
edition = "2021"
authors = ["Turners Dumper"]
description = "Turners Dumper | Discord @grfq"

[profile.release]
opt-level = 3
lto = true
strip = "symbols"
codegen-units = 1
panic = "abort"

[dependencies]
libc = "0.2"
